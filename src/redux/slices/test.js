// sử dụng lệnh rx + Slice để tạo nhanh 1 reducers
import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const test = createSlice({
    name: second,
    initialState,
    reducers: {},
});

export const {} = test.actions;

export default test.reducer;
